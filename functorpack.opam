opam-version: "2.0"
name: "functorpack"
dev-repo: "git+ssh://github.com:remixlabs/functorpack.git"
depends: [
  "omake" {build}
]
build: [
  [make]
  [make "test"] {with-test}
]
install: [
  make "install"
]
